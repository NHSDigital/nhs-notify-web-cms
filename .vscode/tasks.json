{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "root-stop-already-running",
      "options": {
        "cwd": "${workspaceFolder}/docs"
      },
      "command": "kill $(lsof -ti :4000); exit 0;",
      "type": "shell"
    }
  ]
}
